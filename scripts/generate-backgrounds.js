// PDF를 모바일/PC용 배경 이미지로 변환하는 스크립트
// Node.js 환경에서 실행: node scripts/generate-backgrounds.js

const fs = require('fs');
const path = require('path');

// 이 스크립트는 브라우저에서 실행되어야 하므로,
// 대신 usePdfBackground에서 직접 처리하도록 수정합니다.

console.log('이 스크립트는 브라우저에서 실행되어야 합니다.');
console.log('대신 usePdfBackground 훅에서 기기 타입에 따라 다른 렌더링을 수행합니다.');

